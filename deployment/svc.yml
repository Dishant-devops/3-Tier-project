apiVersion: v1
kind: Service
metadata:
  name: 3-tier-app-service
  namespace: 3-tier
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
spec:
  type: ClusterIP
  selector:
    app: 3-tier-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP